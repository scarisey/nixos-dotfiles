name: "Hyperion deployments"
on:
  workflow_dispatch:
    inputs:
      target:
        description: "Test or Prod"
        required: true
        type: choice
        options:
          - Test
          - Prod
jobs:
  deploy-test-from-main:
    if: ${{ github.event.inputs.target == 'Test' }}
    runs-on: ubuntu-latest
    env:
      GIT_AUTHOR_NAME: github-actions[bot]
      GIT_AUTHOR_EMAIL: github-actions[bot]@users.noreply.github.com
      GIT_COMMITTER_NAME: github-actions[bot]
      GIT_COMMITTER_EMAIL: github-actions[bot]@users.noreply.github.com
    permissions:
      actions: write
      contents: write
    steps:
      - uses: actions/checkout@v6
      - name: Generate a token
        id: generate-token
        uses: actions/create-github-app-token@v3
        with:
          app-id: ${{ secrets.GH_APP_ID }}
          private-key: ${{ secrets.GH_APP_PRIVATE_KEY }}
      - name: Reset hyperion/test branch on main
        env:
          GH_TOKEN: ${{ steps.generate-token.outputs.token }}
        run: |
          git remote remove origin
          git remote add origin https://${GH_TOKEN}@github.com/${{ github.repository }}.git
          git fetch
          git checkout -b hyperion/test || git checkout hyperion/test
          git reset --hard origin/main
      - name: Push force
        env:
          GH_TOKEN: ${{ steps.generate-token.outputs.token }}
        run: |
          git push https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }}.git hyperion/test --force
  deploy-prod-from-test:
    if: ${{ github.event.inputs.target == 'Prod' }}
    runs-on: ubuntu-latest
    env:
      GIT_AUTHOR_NAME: github-actions[bot]
      GIT_AUTHOR_EMAIL: github-actions[bot]@users.noreply.github.com
      GIT_COMMITTER_NAME: github-actions[bot]
      GIT_COMMITTER_EMAIL: github-actions[bot]@users.noreply.github.com
    permissions:
      actions: write
      contents: write
    steps:
      - uses: actions/checkout@v6
      - name: Generate a token
        id: generate-token
        uses: actions/create-github-app-token@v3
        with:
          app-id: ${{ secrets.GH_APP_ID }}
          private-key: ${{ secrets.GH_APP_PRIVATE_KEY }}
      - name: Reset hyperion/prod branch on hyperion/test
        env:
          GH_TOKEN: ${{ steps.generate-token.outputs.token }}
        run: |
          git remote remove origin
          git remote add origin https://${GH_TOKEN}@github.com/${{ github.repository }}.git
          git fetch
          git checkout -b hyperion/prod || git checkout hyperion/prod
          git reset --hard origin/hyperion/test
      - name: Push force
        env:
          GH_TOKEN: ${{ steps.generate-token.outputs.token }}
        run:
          git push https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }}.git hyperion/prod --force
